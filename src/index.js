import cipher from './cipher.js';

const offset = document.getElementById('offset_input').value;

const textMessage = document.getElementById('texto_input')


// cipher.encode();
// cipher.decode();

console.log(cipher);